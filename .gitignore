# --- Legacy: local guide PDF tooling (no package.json in repo anymore) ---
node_modules/

# --- Python / IDE ---
__pycache__/
.pytest_cache/
.idea/
*.swp
*.swo
.cursor/
.ipynb_checkpoints/

# --- Secrets / local env ---
.env
.env.*
!.env.example
.venv/
venv/
env/

# --- PDFs in repo root ---
/*.pdf

# --- macOS ---
**/.DS_Store

# --- Extracted frames only (large; run extract_frames.py) ---
data/frames/**

# --- Labels (local; replaced by new CVAT export — not pushed to git) ---
labels/

# --- Probe / label debug artifacts (regenerate: probe_clips.py) ---
debug/

# --- Manual labeling: generated packs (rebuild). Keep downloaded exports you use. ---
labelling/Vehicle.v1i.yolov8/
labelling/cvat/
labelling/roboflow/roboflow/
labelling/roboflow/roboflow_upload/
labelling/roboflow/roboflow_ui_upload_8457857/

# --- Training & evaluation outputs (regenerate: train.py, evaluate.py) ---
outputs/*
!outputs/eval_metrics.json
!outputs/metrics_table.md
!outputs/eval_videos/
outputs/eval_videos/*
!outputs/eval_videos/*_predictions.mp4
!outputs/probe_frames/
!outputs/probe_frames/**
outputs/probe_frames/**/.DS_Store

# --- Model weights ---
weights/                          # CLIP etc. (large, download locally)
yolov8x-worldv2.pt                # 140 MB pseudo-labeler (download)
yolo11s.pt                        # pretrained base (download via Ultralytics)
*.pt
*.pth
*.onnx
*.engine
# Fine-tuned deliverables (local only — never commit)
checkpoints/

# Ultralytics run folders (if created outside outputs/)
runs/

# --- Local-only ad-hoc scripts at repo root (not committed) ---
# Tracked: config/experiments/ (round settings) and src/training/experiments/ (runners).
# Pack blobs stay gitignored under data/datasets/; outputs under outputs/.
/experiments/

# --- MLflow (local tracking; not in git) ---
mlruns/
mlflow.db
.mlflow/
mlflow_server/

# --- Dataset packs: local only (never commit blobs) ---
data/datasets/**
data/dataset/

data/raw_export/
data/raw_base/
data/cvat/
config/cvat_tasks.json

# --- Autolabel demo outputs ---
outputs/autolabel/


# --- Matplotlib / cache dirs ---
.matplotlib/
**/.matplotlib_cache/

# --- Android bench app build ---
# Android bench — Gradle project (local only, not in git). Ship ../vehicle-bench.apk
src/optimisation/android/app/
src/optimisation/android/logs/
!src/optimisation/android/vehicle-bench.apk
