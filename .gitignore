# --- Guides (local docs / PDF export) ---
guides/

# --- Node (guide PDF export) ---
node_modules/

# --- Python / IDE ---
__pycache__/
.pytest_cache/
.idea/
*.swp
*.swo

# --- Secrets / local env ---
.env
.env.*
.venv/
venv/
env/

# --- PDFs in repo root ---
/*.pdf

# --- macOS ---
.DS_Store

# --- Extracted frames only (large; run extract_frames.py) ---
data/frames/**

# --- Pseudo-labels (regenerate: autolabel_yworld.py) ---
labels/

# --- Probe / label debug artifacts (regenerate: probe_clips.py, autolabel) ---
debug/

# --- Manual labeling packs (large / regenerable; keep scripts + README) ---
labelling/Vehicle.v1i.yolov8/
labelling/cvat/coco_export/
labelling/cvat/label_man/
labelling/roboflow/roboflow/
labelling/roboflow/roboflow_upload/
labelling/roboflow/roboflow_ui_upload_8457857/
labelling/roboflow/Vehicle_roboflow/

# --- Training & evaluation outputs (regenerate: train.py, evaluate.py) ---
outputs/*
!outputs/eval_metrics.json
!outputs/metrics_table.md
!outputs/eval_videos/
outputs/eval_videos/*
!outputs/eval_videos/*_predictions.mp4

# --- Model weights ---
weights/                          # CLIP etc. (large, download locally)
yolov8x-worldv2.pt                # 140 MB pseudo-labeler (download)
yolo11s.pt                        # pretrained base (download via Ultralytics)
*.pt
*.pth
*.onnx
*.engine
# Fine-tuned deliverable — copy after train:
#   cp outputs/runs/yolo11s_vehicle/weights/best.pt checkpoints/yolo11s_vehicle_best.pt
!checkpoints/
checkpoints/*
!checkpoints/yolo11s_vehicle_best.pt

# Ultralytics run folders (if created outside outputs/)
runs/

# --- Local-only experiment scripts (not committed) ---
experiments/

# --- MLflow / DVC (coming next; keep remotes/pointers in git later) ---
mlruns/
mlflow.db
.mlflow/
.dvc/cache/
.dvc/tmp/
.dvc/tmp/**
*.dvc.bak

# --- Matplotlib / cache dirs ---
.matplotlib/
**/.matplotlib_cache/
