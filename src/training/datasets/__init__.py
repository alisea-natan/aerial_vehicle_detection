"""Dataset pack builders (tiling / sampling variants)."""

from training.datasets.specs import (  # noqa: F401
    VARIANTS_PATH,
    VariantSpec,
    list_variant_ids,
    load_variants_config,
    resolve_labels_root,
    resolve_variant,
)
