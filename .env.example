# Local secrets only — copy once: cp .env.example .env
# Loaded automatically when Python imports common.config. Do not commit .env.

# CVAT (src/labeling/cvat_pull.py)
CVAT_HOST=https://app.cvat.ai
CVAT_USER=
CVAT_PASS=
CVAT_PROJECT=

# Optional — only if you still use Roboflow scripts
ROBOFLOW_API_KEY=
