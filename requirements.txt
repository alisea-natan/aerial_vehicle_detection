# Pinned for reproducible PoC results.
# Python: use 3.12. Supported range for these pins: >=3.10, <=3.14.
# Create venv: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
opencv-python==4.13.0.92
ultralytics==8.4.62
sahi==0.12.5
matplotlib==3.10.7
numpy==2.1.3
lap==0.5.13
PyYAML==6.0.3
torch==2.12.0
torchvision==0.27.0

# YOLO-World / Ultralytics text encoder (OpenAI CLIP fork).
# ultralytics installs this on first World-model use; pin so fresh venvs work offline-ish.
clip @ git+https://github.com/ultralytics/CLIP.git@488e81a6711eea7346872b46ea928b367da8889d
ftfy==6.3.1
regex==2026.7.19

# Load .env (CVAT_*, ROBOFLOW_*, …)
python-dotenv==1.1.1

# Optional: CVAT export (src/labeling/cvat/cvat_pull.py). Pin near your server (2.70.x).
cvat-sdk==2.70.0

# Optimisation (src/optimisation/ — OpenVINO/ONNX export, INT8, prune).
# onnxslim: Ultralytics export AutoUpdates it if missing — pin so the run does not pip mid-job.
onnx==1.22.0
onnxruntime==1.29.0
openvino==2026.3.0
openvino-telemetry==2025.2.0
onnxslim==0.1.96
# protobuf <6 for tensorflow; >=4.25.8 for onnxruntime.
protobuf==5.29.5
ml_dtypes==0.6.0
flatbuffers==25.12.19
torch-pruning==1.6.1
# INT8 OpenVINO export (Ultralytics AutoUpdates nncf if missing — pin so step 2 does not pip mid-job).
nncf==3.3.0
# TFLite export in run_export.py (A1). Ultralytics AutoUpdates these if missing — pin so export does not pip mid-job.
tensorflow==2.19.0
onnx2tf==1.28.8
tf-keras==2.19.0
sng4onnx==1.0.5
onnx-graphsurgeon==0.5.8
ai-edge-litert==1.3.0
